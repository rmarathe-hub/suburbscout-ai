services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: suburbscout
      POSTGRES_USER: suburbscout
      POSTGRES_PASSWORD: suburbscout_dev
    ports:
      - "5432:5432"
    volumes:
      - suburbscout_pg:/var/lib/postgresql/data

volumes:
  suburbscout_pg:
